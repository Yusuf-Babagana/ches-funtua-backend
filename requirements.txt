# django-filter's upper bound below matters: django-filter>=25.0 hard-requires
# Django>=5.2 in its own metadata, which would silently pull Django 5.2 into a
# fresh install despite the Django constraint here -- this whole project has
# only ever been built and tested against Django 4.2.x. Bump both together
# deliberately, not independently.
Django>=4.2,<5.3
djangorestframework>=3.14,<4.0
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.2,<25.0

numpy>=1.26,<2.1
pandas>=2.1,<2.4
pillow>=10.0
openpyxl>=3.1,<3.2
reportlab>=4.0,<4.5

requests>=2.31

psutil>=5.9
PyJWT>=2.8

asgiref>=3.7
sqlparse>=0.4
tzdata>=2024.1
